package com.aluracursos.backend.dominio.topico;

/**
 * Enum que representa los estados posibles de un tópico en el sistema.
 */
public enum TopicoStatus {
    ACTIVO,
    CERRADO,
    NO_RESPONDIDO,
    RESPONDIDO
}
